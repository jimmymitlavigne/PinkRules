# 0.3.1 验证记录

日期：2026-09-25。环境：macOS、Node.js、无头 Chrome，390 × 844 手机视口。

## 本次抓包回放

只读分析用户提供的 QX 导出；不发布原始数据，也不把正文发送到外部翻译服务。

| 检查 | 结果 |
|---|---|
| 导出请求总数 | 2,778 |
| FT __origami/service/image 图片请求 | 804；新版正文规则命中 0 |
| NYT Asset 文章响应 | 26；全部成功注入且可重新解析 JSON |
| NYT 栏目响应 | 1；保持不变 |
| NYT 空 Asset 响应 | 1；无可回放正文 |
| JSON 数据完整性 | 26 篇逐一校验；除 HTML 外字段全部一致，重复处理不重复注入 |
| 浏览器离线回放 | 26/26 页面仅出现一个翻译控件按钮；879 个段落完成模拟翻译 |
| 按需翻译与缓存 | 打开文章不自动翻译；切回原文再显示译文不新增请求 |
| 回放页面外部请求 | 0；路由全拦截，原站脚本不运行 |

回放入口：tools/replay-capture.cjs。输入为解压后的 QX 导出目录，HTTP 分块传输、gzip 和 Brotli 在测试工具中解码；QX 实际重写使用其提供的文本正文。

87 条 HTTP 分块和 25 条 Brotli 响应补充解码后，全包解码错误为 0。编号范围为 843–3620。WSJ 的令牌响应仅含 tokens/errors/maxRedemptions/expiresTimestamp 字段；FT 的推荐等接口不含正文；全包未找到经济学人正文。NYT 另有一条网页响应为 HTTP 403，脚本保持原样，诊断页记录跳过状态。

## 自动检查

- 核心测试 10 项：HTML/JSON 分流、NYT Article 校验、参数顺序、重复注入、CSP nonce、单 JS 路由、服务请求协议、密钥隔离、输入校验、限流/超时、诊断记录不保存正文或密钥。
- 浏览器人工样本：四家主域名、CSP、长段落、隐藏内容、四种服务切换、缓存切换、部分失败续译、切页取消旧结果。
- 单按钮操作、正文 hidden 属性解除后显示按钮、控制节点被移除后恢复曾通过浏览器检查。随后按用户要求改为“显示双语 / 显示原文”，翻译中再次点击也隐藏译文。最终双态交互和诊断页的新增检查尚未在浏览器重跑：启动 Chrome 的自动审批因服务额度不足未完成；语法、核心测试和正文改写回放另行完成。服务配置通过 BoxJS 读取，正文不显示服务菜单。
- 0.3.0 已完成 Google 两句人工文本的真实接口测试；本轮不发送抓包正文进行联网翻译。
- 原订阅地址保持不变，远程 JS 和 HTML 内客户端路径增加 v=0.3.1 参数。

## 仍需真机验证

- 0.3.1 在 iPhone QX 和 NYT App 中的实际加载、按钮显示、点击翻译；离线浏览器测试不能覆盖原站脚本、App WebView、缓存和证书行为。
- FT 的正文网络入口：本次导出未找到正文响应。缓存或未导出的请求是可能原因，尚不能确定。
- WSJ 与 Economist 的 App 正文结构；本次导出不足以适配。
- DeepL、Gemini、DeepSeek 的真实付费 API 调用，以及 BoxJS 的手机端导入。

交付状态：用户要求四款 iOS App 全部支持；目前尚未满足，只交付本轮有证据的适配更新供复测。
